<%= form_for(contender) do |f| %>
  <% if contender.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contender.errors.count, "error") %> prohibited this contender from being saved:</h2>

      <ul>
      <% contender.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :tournament_id %>
    <%= f.number_field :tournament_id %>
  </div>

  <div class="field">
    <%= f.label :deck_id %>
    <%= f.number_field :deck_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
