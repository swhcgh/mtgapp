<%= form_for(deck) do |f| %>
  <% if deck.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(deck.errors.count, "error") %> prohibited this deck from being saved:</h2>

      <ul>
      <% deck.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contender_id %>
    <%= f.number_field :contender_id %>
  </div>

  <div class="field">
    <%= f.label :Cardlist %>
    <%= f.text_area :Cardlist %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
